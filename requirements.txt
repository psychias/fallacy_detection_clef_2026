# Touché 2026 Fallacy Detection — UZHCL_for_the_win
#
# Tested on Python 3.10, CUDA 12.1 (A100-40GB).
# torch + bitsandbytes wheels are platform-specific; install torch first
# matching your CUDA version (see https://pytorch.org/get-started/locally/).

# Core training stack (RoBERTa-large fine-tuning)
torch>=2.0,<2.6
transformers>=4.48,<4.53
datasets>=2.16
accelerate>=0.26
tokenizers>=0.15
safetensors>=0.4

# Eval + utility
scikit-learn>=1.3
numpy>=1.24
tqdm
psutil

# Synthetic-data generation (OpenRouter-backed; requires OPENROUTER_API_KEY)
openai>=1.10

# LLM detour (Qwen 7B/32B CoT-LoRA — optional, only needed for reproducing §3.5)
sentence-transformers>=2.2
peft>=0.9
bitsandbytes>=0.43
torchao>=0.16
